export const signUp = (req, res) => {
  res.json({
    data: "SignUp route",
  });
};

export const login = (req, res) => {
  res.json({
    data: "Login route",
  });
};

export const logout = (req, res) => {
  res.json({
    data: "Logout route",
  });
};
